const toContentBlock = parentStyles => ({
    root: parentStyles.contentBlock,
    title: parentStyles.contentBlockHeader,
    text: parentStyles.contentBlockText,
    button: parentStyles.contentBlockCta
});

export default toContentBlock;
