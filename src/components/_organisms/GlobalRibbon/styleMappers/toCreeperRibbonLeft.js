const toCreeperRibbonRight = parentStyles => ({
    root: parentStyles.root,
    wrapper: parentStyles.wrapper,
    left: parentStyles.left,
    right: parentStyles.right    
});

export default toCreeperRibbonRight;
