const toBgMediaTile = parentStyles => ({
    root: parentStyles.item,
    header: parentStyles.itemHeader,
    title: parentStyles.itemTitle,
    text: parentStyles.itemText,
    content: parentStyles.itemContent,
    description: parentStyles.itemDescription,
    button: parentStyles.itemCta
});

export default toBgMediaTile;
